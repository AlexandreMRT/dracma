<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Dracma" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-900 text-gray-100 min-h-screen">
    <% if logged_in? %>
      <nav class="bg-gray-800 border-b border-gray-700 px-4 py-3">
        <div class="container mx-auto flex items-center justify-between">
          <div class="flex items-center space-x-6">
            <%= link_to "Dracma", root_path, class: "text-xl font-bold text-emerald-400" %>
            <%= link_to "Assets", assets_path, class: "text-gray-300 hover:text-white text-sm" %>
            <%= link_to "Quotes", quotes_path, class: "text-gray-300 hover:text-white text-sm" %>
            <%= link_to "Watchlist", watchlists_path, class: "text-gray-300 hover:text-white text-sm" %>
            <%= link_to "Portfolios", portfolios_path, class: "text-gray-300 hover:text-white text-sm" %>
            <%= link_to "Exports", exports_path, class: "text-gray-300 hover:text-white text-sm" %>
          </div>
          <div class="flex items-center space-x-4">
            <span class="text-gray-400 text-sm"><%= current_user.name %></span>
            <%= button_to "Logout", logout_path, method: :delete, class: "text-gray-400 hover:text-white text-sm" %>
          </div>
        </div>
      </nav>
    <% end %>

    <% if flash[:notice] %>
      <div class="container mx-auto px-5 mt-4">
        <div class="bg-emerald-900/50 border border-emerald-600 text-emerald-200 px-4 py-2 rounded text-sm">
          <%= flash[:notice] %>
        </div>
      </div>
    <% end %>
    <% if flash[:alert] %>
      <div class="container mx-auto px-5 mt-4">
        <div class="bg-red-900/50 border border-red-600 text-red-200 px-4 py-2 rounded text-sm">
          <%= flash[:alert] %>
        </div>
      </div>
    <% end %>

    <main class="container mx-auto mt-6 px-5 pb-10">
      <%= yield %>
    </main>
  </body>
</html>
